# FST System Scaffold (API + worker + DB + React)
- Subes video (NO analiza)
- Presionas "Analizar" -> crea job
- Worker procesa y guarda resultados

Run:
docker compose up --build

API: http://localhost:8000/health
UI:  http://localhost:5173
